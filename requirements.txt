# GoofishMasterDesktop 依赖清单（合并 4 个服务 + 桌面壳）
# 安装：pip install -r requirements.txt

# ---- 4 个服务基础依赖（来自各自 requirements.txt）----
lark-oapi>=1.3.0
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
segno>=1.6
websockets>=12.0
pydantic>=2.0
python-dotenv>=1.0
pyotp>=2.9
cryptography>=42.0
redis>=5.0
openai>=1.0
pyyaml>=6.0

# ---- P1 单用户化：外部 DB 全部改为进程内嵌入式实现 ----
aiosqlite>=0.19      # Postgres -> SQLite（文件库）
fakeredis>=2.20      # Redis -> 进程内内存兼容层
chromadb>=0.5        # Qdrant -> 本地向量库（PersistentClient 落盘）
playwright>=1.40
requests>=2.31
aiofiles>=23.0
Pillow>=10.0
python-socks>=2.4
pydantic-settings>=2.0

# ---- 桌面壳 ----
pywebview>=5.0
pystray>=0.19
